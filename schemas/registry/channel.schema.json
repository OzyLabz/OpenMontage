{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "openmontage/registry/channel",
  "title": "Channel Entry",
  "description": "Faceless-mode channel entry. Locks style (playbook), voice, and format (media profile) for the existing pipeline. Does NOT name a model or pin a gateway — the existing scorer chooses generation models (switch OFF). `style_prompt` and `niche` are conditioning-only (prompt text); request matching uses `match.keywords` only.",
  "type": "object",
  "required": ["id", "match", "pipeline", "playbook", "voice", "media_profile"],
  "properties": {
    "id": {
      "type": "string",
      "description": "Canonical channel id (kebab-case).",
      "pattern": "^[a-z0-9][a-z0-9-]*$"
    },
    "match": {
      "type": "object",
      "description": "Request-matching config. Matching uses keywords only (dead-simple for this build).",
      "required": ["keywords"],
      "properties": {
        "keywords": {
          "type": "array",
          "description": "Lowercase phrases; a request matches this channel if it contains any of them.",
          "items": { "type": "string", "minLength": 1 },
          "minItems": 1
        }
      },
      "additionalProperties": false
    },
    "pipeline": {
      "type": "string",
      "description": "Pipeline manifest name (without .yaml) in pipeline_defs/."
    },
    "playbook": {
      "type": "string",
      "description": "Style playbook name (without .yaml) in styles/."
    },
    "voice": {
      "type": "object",
      "required": ["provider", "id"],
      "properties": {
        "provider": {
          "type": "string",
          "description": "TTS provider name (e.g. google, elevenlabs)."
        },
        "id": {
          "type": "string",
          "description": "Provider-specific voice id passed as the TTS `voice` input."
        }
      },
      "additionalProperties": false
    },
    "media_profile": {
      "type": "string",
      "description": "Media profile key in lib/media_profiles.py (e.g. youtube_landscape)."
    },
    "style_prompt": {
      "type": "string",
      "description": "Conditioning-only. Concatenated into generation prompts; never read in a conditional or routing decision."
    },
    "niche": {
      "type": "string",
      "description": "Conditioning-only descriptor. Flavor text for prompts; never branched on. NOT used for matching."
    },
    "visuals": {
      "type": "object",
      "description": "Optional. OFF-path gateway/vendor bias for AI-generated visuals (NOT a model pin; switch stays OFF). Each value must equal a discovered tool's `provider` id for that capability (e.g. 'kie'); threaded into image_selector/video_selector as `preferred_provider` at the asset stage. If that provider is unavailable, the selector falls through to the scorer.",
      "properties": {
        "image_provider": { "type": "string", "description": "Preferred image_generation provider id (e.g. kie)." },
        "video_provider": { "type": "string", "description": "Preferred video_generation provider id (e.g. kie)." }
      },
      "additionalProperties": false
    }
  },
  "additionalProperties": false
}
